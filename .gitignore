# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
dist/
.coverage

# Node (web/, day 4)
node_modules/
.next/
*.tsbuildinfo

# Env and secrets - keys live in Railway/Vercel env, never in the repo
.env
.env.*
!.env.example

# OS
.DS_Store

# Separate embedded tooling repo, not part of this project
hive/
roster.json
roster-backups/

# Commit-message and PR-body scratch files. Agents write these next to the
# repo root and reach for `git add -A`, which is how one of them ended up
# committed into four commits of #152 before a diff against the pre-reshape
# head caught it.
.commitmsg
.prbody
