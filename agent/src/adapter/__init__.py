"""LiveKit adapter for the Binghatti ambassador.

Framework wiring only. Every claim the demo makes is implemented in
`ambassador/`, which imports nothing from here and nothing from LiveKit
(ADR-002). If logic starts accumulating in this package, it is in the wrong
place.
"""
